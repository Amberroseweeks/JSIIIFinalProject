import react from "react";

const ActivityIconBar = (props) =>{

    return(
        <div class="activityiconbarcontainer">
<p>This works!!{props.icon} {props.activites}</p>
        </div>

    )

}

export default ActivityIconBar;